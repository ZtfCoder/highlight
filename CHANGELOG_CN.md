# 更新日志

## 3.0.1
-----------------
1. 新增批量导入模式 - 添加高亮词时支持按换行、逗号、空格分隔批量添加，自动去重
2. 新增统一样式设置 - 编辑分组时可一键将统一样式应用到分组内所有高亮词
3. 导入时支持预设样式 - 导入高亮词时可预先设置统一的颜色和样式
4. 批量导入实时预览 - 添加高亮词时可预览即将批量添加的内容

## 3.0.0
-----------------
🎉 重大更新
- 全新国际化支持 - 新增中英文双语切换，自动适配浏览器语言设置
- 全新 UI 架构 - 使用 React + TypeScript 重构弹窗界面，交互更流畅
- 分组管理升级 - 支持创建、编辑、删除高亮词分组，分类更清晰
- 样式自定义增强 - 每个高亮词可独立设置颜色、加粗、下划线等样式
- 导入/导出优化 - 支持 JSON 格式批量导入导出，兼容 v2 版本配置
- Shadow DOM 支持 - 完美兼容 Shadow DOM、微前端等复杂网页结构
- 性能优化 - 修复内存泄漏问题，长时间使用更稳定

⚠️ 升级提示
- v3.0.0 为重大版本更新，数据结构有变化
- 请先在 v2.1.2 中导出配置备份，再升级到 v3.0.0 后导入

## 2.1.2
-----------------
1. 新增 v2 导出 json 配置，为 v3 版本导入做铺垫

tip：请更新到 v2.1.2 的用户首先导出自己的 json 配置以作备份，避免 3.0.0 版本更新导致数据丢失

## 2.1.1
-----------------
1. 提供批量添加（根据换行、空格）分割批量添加
2. 不修改网页源代码，对网站兼容性极高
